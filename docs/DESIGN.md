# ニュース連想モデル 設計書 v1.1

- ステータス: **設計確定(コード未着手)**。2026-09-25
- 位置づけ: `CONCEPT.md` v2.0 を**どう作るか**を定める。何を・なぜ作るかは `CONCEPT.md` を正とする
- 旧版(v0.3)からの主な変更は付録Aに記載する。旧版は git の履歴に残っている

---

## 1. 全体構成

### 1.1 処理の流れ

```
[夕方・自動]  Python
  1  収集          RSS・Google News・GDELT・Wikipedia閲覧数・TDnet・EDINET・上場銘柄一覧・株価
  2  イベント化    同じ出来事の記事をまとめ、イベント候補にする
  3  足切り        注目度・新規性・開示の種類で 0〜3件に絞る(進行中シナリオの関係分は別枠)
  4  株価の計算    騰落率・出来高倍率・普段の値動きの幅・TOPIX 超過
  5  入力パック    LLM に渡す材料を1つの JSON にまとめる

[夜・手動]  Claude Code(/association-daily)
  6  等級付け(暫定)   S/A/B/C
  7  シナリオの照合    既存シナリオの強化・弱体化・繰り返し・消滅、または新しいシナリオ
  8  シナリオを立てる  時間の型、起動・終了の条件、連想(2つの視点)、候補、想定上昇幅、
                      過去事例の参照、確認計画
  9  裏取り            上位1〜2件だけ。反証も調べる
  10 出力              スキーマに沿った JSON

[夜・自動]  Python(コマンドの最後に呼ぶ)
  11 確定              スキーマの検証、銘柄コードと社名の照合、信頼度の確認、時刻の付与、追記
  12 計算              鮮度・織り込み度・反応の診断・自信度・本命の判定・除外
  13 レポート          O1 連想レポート・O3 連携ファイル

[翌朝・自動]  米国市場の1段目の反応を確認し、閾値を超えたら通知する
[翌日の引け後・自動]
  14 等級の確定        1段目の反応で検算
  15 状態の更新        織り込み度・鮮度・終了判定・売りのサイン・「動いた」の判定

[月1回]  Python の集計 + Claude Code(/association-monthly)
  16 振り返り          O2 振り返りレポート(見逃し・外れ・3つの指標・改善の実行)
```

### 1.2 担当の分け方

- **Python**:事実と数字を扱う(収集、計算、時刻、照合、確定、集計)。
- **LLM**:発想と解釈を扱う(等級、連想、確認計画、裏取りの要約、反証)。
- LLM が出した数値(想定上昇幅、実現確度など)は「推定値」として記録する。**時刻と株価に基づく数値は、必ず Python が計算する。**

---

## 2. 不変条件(全体に共通する決まり)

1. **いつ知り得たかを記録する**:すべての記録に `first_observed_at`(本システムが最初に取得した時刻)を持たせる。判断に使ってよいのは、その判断の時点までに取得済みの情報だけとする。ソース側の公開時刻(`published_at`)は後から書き換わることがあるので、判定には使わない。
2. **時刻は UTC で保存する**:表示だけ日本時間にする。営業日の判定は、東証の営業日カレンダー(既存システムにあれば再利用)で行う。鮮度や想定期間は営業日で数える。
3. **追記だけで記録する**:シナリオ・候補・ランキング・裏取りの記録は書き換えない。変えるときは新しい版を追記する。
4. **落としたものも記録する**:足切りで落としたイベント、C級・B級の判定、候補にしなかった銘柄も理由とともに残す。
5. **LLM の推論は証拠にしない**:証拠は URL・取得時刻・値・信頼度がそろったものだけとする。
6. **使ったモデルと指示文の版を記録する**:実行ごとに `model_id` と `prompt_version` を残す。
7. **既存資産を壊さない**:既存システムのコードとデータ(yfinance の株価DBを含む)は読み取り専用で使う。
8. **件数は推測しない**:報告に書く件数は、すべて機械的な集計による。
9. **過去事例と実運用の記録を分ける**:過去事例ライブラリ(学習用)と、実運用の予測の記録(能力の測定用)は別に保存する。過去事例を LLM に解かせた結果は、能力の測定に使わない。

---

## 3. データソース(すべて無料)

| 用途 | ソース | 頻度 | 過去分 | 注意点 |
|---|---|---|---|---|
| 国内ニュース | NHK・主要紙の RSS | 1時間ごと | 取れない | 本文は保存しない(見出し・URL・要約・固有表現) |
| 報道の広がり | Google News RSS(キーワード指定) | 1日数回 | 取れない | 媒体数を数える |
| 世界の報道量 | GDELT | 1日1回 | 2015年〜 | 過去事例の注目度も再現できる |
| 一般の関心 | Wikipedia 閲覧数 API(日本語版・英語版) | 1日1回 | 2015年7月〜 | 公式で安定 |
| 検索の関心 | Google トレンド | 必要時 | 取れる | 非公式ライブラリで不安定。補助のみ |
| 適時開示 | TDnet | 1時間ごと | **約1か月で消える** | 最優先で収集を始める |
| 大量保有・有価証券報告書 | EDINET API | 1日1回 | 取れる | API キーが必要 |
| 株価・出来高 | yfinance(既存のローカルDB) | 既存の更新に従う | 取れる | 読み取りのみ。候補に使った銘柄は本システムにも複製する |
| 米国の1段目 | yfinance | 翌朝 | 取れる | 米国市場の終値を確認する |
| 上場銘柄一覧 | JPX | 1日1回 | 取れない | 毎日保存し、その時点の上場銘柄を記録する |
| 決算発表の予定 | TDnet・各社の公表 | 1日1回 | ― | 取れない場合は前年の同時期から推定し、推定と明記する |
| 権利落ち日 | 決算期末からの計算 | ― | ― | ― |
| 信用規制 | JPX の公表 | 1日1回 | ― | 日々公表銘柄・増担保規制 |
| 監理・整理ポスト | JPX の公表 | 1日1回 | ― | 本命の除外に使う |
| 信用残 | 既存の需給システムのデータ | ― | ― | S1 で所在を確認する |
| テーマの参考 | 株探・kabukarin.net など | 週1回(人) | ― | 自動取得は利用規約の確認後。不可なら人が読むだけ |
| 裏取り用の統計 | 気象庁、農林水産省、e-Stat、経済産業省、電力広域的運営推進機関 など | 必要時 | ― | 公表日時も記録する |

---

## 4. 保存の方針

- **git で管理するもの**:コード、設定、指示文、スキーマ、文書、テーマ一覧、前提カード、過去事例ライブラリ(小さな JSON・Markdown)。
- **git に入れないもの**:生データ、DB、レポート(`data/` に置き、`.gitignore` で除外する)。
- 生データ以外は、スクリプトで作り直せるようにする。
- **作り直せない記録**(予測の記録、裏取りの記録、実行の記録)は追記専用の JSONL とし、1行ごとにハッシュを付ける。これだけは git 以外の場所にも毎日複製する(バックアップ)。
- DB は DuckDB(1ファイルで高速に集計できる)。
- ニュースの本文は保存しない(容量と著作権のため)。

---

## 5. データモデル

主要な記録と項目の要点を示す。正式な定義は `schemas/` の JSON スキーマで行う。

```
news_item        : news_id, source, source_tier(1-4), title, summary, url,
                   language, published_at, first_observed_at, entities[], text_hash
event            : event_id, title, news_ids[], entities[], first_observed_at,
                   attention{media_count, gdelt, wiki}, disclosure_type,
                   novelty_hash, gate{passed, reason}
theme            : theme_id, name, category, temperature(上昇中|横ばい|冷却中),
                   temperature_updated_at, premise_card_version, is_new_theme_candidate
premise_card     : theme_id, version, created_at, market_premise, key_numbers[],
                   breaking_directions[], based_on_event_id
grade            : event_id, provisional_grade(S|A|B|C), provisional_at,
                   answers{new_fact, distance_from_premise, source_certainty},
                   premise_card_version, final_grade, final_at,
                   check{first_tier_tickers[], move_vs_normal}
scenario         : scenario_id, version, statement, theme_ids[], new_theme_flag,
                   time_type(単発|局面展開|予定日), phase, start_condition,
                   end_condition, expected_days, status(待機|進行中|休眠|終了),
                   started_at, strength(Weak|Weak+|Medium|Strong),
                   expected_rise{low, median, high, version},
                   past_case_ids[], created_at, model_id, prompt_version
scenario_update  : scenario_id, event_id, type(強化|弱体化|繰り返し|消滅),
                   new_fact_summary, new_fact_hash, reading_for, reading_against,
                   strength_before, strength_after, recorded_at
check_plan       : scenario_id, arrow_no, arrow, indicator, source, criterion,
                   next_release_at, status(確認|否定|未確認), created_at
evidence         : evidence_id, scenario_id, arrow_no, url, retrieved_at,
                   value, source_tier(1-4), supports(for|against)
candidate        : candidate_id, scenario_id, ticker, company_name, stage(1段目|2段目|3段目以降),
                   viewpoint(経済|投資家|両方), rationale, break_conditions[],
                   next_catalyst, realization_prob, direction_clear, created_at
ranking_snapshot : date, candidate_id, rank, confidence, realization_prob,
                   remaining_room, freshness, priced_in, diagnosis,
                   tier(本命|監視), excluded_reason, stars
pick_tracking    : candidate_id, first_pick_date, entry_ref_price, status,
                   sell_signals[], moved(bool), moved_at, days_to_move
user_decision    : candidate_id, action(買う|監視|見送り), reason, decided_at(任意)
past_case        : case_id, event_summary, event_date, theme_ids[], time_type,
                   related[{ticker, stage, moved, max_excess, days_to_move}],
                   notes, created_at
run_log          : date, executed, mode(通常|最小), duration_min, skip_reason,
                   model_id, prompt_version
universe_daily   : date, ticker, company_name, market, industry33, status(監理|整理|通常)
company_master   : ticker, company_name, aliases[], industry33, market_cap_band
calendar         : ticker, earnings_date(推定か), ex_rights_date, credit_restriction
price_copy       : ticker, date, ohlcv(候補に使った銘柄の複製)
```

---

## 6. 処理の詳細

### 6.1 収集(Python・自動)

- 各ソースを所定の頻度で取得し、`first_observed_at` を付けて保存する。
- 取得に失敗した場合は記録し、次回に再試行する。3回続けて失敗したら、レポートの冒頭に警告を出す。
- **LLM の夜の手順を実行しなかった日も、収集は続ける。**

### 6.2 イベント化と足切り(Python・自動)

1. 見出しと固有表現の類似度で、同じ出来事の記事を1つのイベントにまとめる。
2. 次のどれかを満たすイベントを通す(閾値は §7)。
   - 注目度(媒体数・GDELT・Wikipedia)が、過去90日のイベントの上位5%
   - 注目度が急に伸びている(前日比の伸びが上位5%)
   - 株価への影響が大きい種類の開示(大量保有報告書、TOB、大手との資本業務提携、業績予想の大幅な修正、国の大型予算・規制の決定)
3. **新規性**:すでに記録した事実と同じ内容(`novelty_hash` が一致)は落とす。
4. 通ったものから注目度の順に**最大3件**を LLM に渡す。進行中のシナリオの対象企業やキーワードに一致するものは、上限の枠外で渡す。
5. 落としたイベントも、理由とともに記録する。

### 6.3 入力パック(Python・自動)

LLM に渡す JSON には、次をまとめる。

- 足切りを通ったイベント(見出し、要約、URL、注目度、開示の種類)
- 関係するテーマの前提カード(最新版)と温度
- 進行中・待機中のシナリオの一覧(最大15件)と、それぞれの確認計画の状況
- 関係しそうな過去事例(テーマと時間の型で検索したもの)
- 候補になり得る銘柄の株価の状態(1段目の反応、出来高、普段の値動きの幅)
- 今日が公表日の統計(確認計画の「次の公表」に当たるもの)

### 6.4 夜の手順(Claude Code・手動)

`/association-daily` として固定する。指示文は `prompts/` で版を管理する。

1. **暫定の等級**:各イベントに S/A/B/C を付ける。4つの質問のうち1〜3に答え、前提カードの数字と新しい数字を並べて示す。
   - 前提カードがないテーマで S・A級の可能性があるときは、**ニュースを見る前の市場の想定を先に書き出して前提カード v1 を作り**、それを基準に等級を付ける。
   - 翌日の検算に使う1段目の銘柄を指定する。1段目に上場企業がない場合(政策・災害など)は、テーマ関連の銘柄群または業種指数を指定する。
2. **シナリオの照合**:進行中・待機中のシナリオそれぞれについて、強化・弱体化・繰り返し・消滅のどれに当たるかを判定する。強化と弱体化の両方の読み方を書いたうえで判定する。
3. **新しいシナリオ**(S・A級のみ):
   - 時間の型、起動条件、終了条件、想定期間(局面展開型は `THEMES.md` のひな形を初期値にする)
   - 連想:経済的なつながりと投資家の連想の2つの視点で、1段目と2段目以降の候補を出す(1シナリオ最大5銘柄。銘柄コードと社名の両方を必須とする)
   - 想定上昇幅(下限・中央・上限)とその根拠
   - 似た過去事例(ライブラリから引く)
   - 確認計画(矢印ごとに、指標・情報源・基準・次の公表日)
   - どのテーマにも当てはまらなければ、新テーマ候補の目印を付ける
4. **裏取り**:その夜の対象は、新しく立てたシナリオと強化されたシナリオのうち、上位1〜2件だけとする。上位の決め方は、S級を優先し、次に裏取り前の推定値(実現確度 × 想定上昇幅の中央値)の大きい順とする。Web 検索で確認計画の矢印を確かめ、反証も調べる。証拠は URL・値・信頼度とともに出力する。
5. **出力**:スキーマに沿った JSON を `data/inbox/` に書き出し、確定処理を呼ぶ。

**最小手順(10分)**:時間がない日は、2(照合)と、注目度が最大の1件の等級付けだけを行う。

### 6.5 確定(Python・自動)

- JSON スキーマで検証する。不合格なら確定せず、理由を表示する。
- **銘柄コードと社名を、銘柄マスタと別名辞書で照合する。** どちらかが合わない候補は確定しない。取り違えの件数も記録する。
- 証拠の URL がない、または信頼度3・4の証拠だけで「確認」とされた矢印は、「未確認」に直す。
- 時刻・モデル・指示文の版を付け、追記する。

### 6.6 計算(Python・自動)

| 項目 | 計算方法 |
|---|---|
| 鮮度 | 起動日からの営業日数 ÷ 想定期間 で減らす(100% → 0%)。待機中は止める。強化された日を新しい起点とする |
| 織り込み度 | 起動日の前日終値からの TOPIX 超過上昇 ÷ 想定上昇幅(中央値)。想定上昇幅の版番号も記録する |
| 反応の診断 | §6.6.1 の規則で1つのラベルにする |
| 自信度 | 実現確度 × 残り余地 × 鮮度の補正(`CONCEPT.md` §8.4) |
| 本命 | 自信度の上位3件。ただし §8 の除外条件に当たるものは本命にしない |
| ★ | 自信度を §7 の固定の閾値で4段階に分ける |

#### 6.6.1 反応の診断のラベル

「注目度」はイベントの注目度、「1段目の反応」は1段目の銘柄の反応、「候補の反応」は候補銘柄自身の反応を指す(判定の閾値は §7)。上から順に当てはめ、最初に当たったものをラベルとする。

| 順 | 条件 | ラベル |
|---|---|---|
| 1 | 候補が反応した(織り込み度80%未満) | 織り込み進行 |
| 2 | 1段目が反応した、候補は反応していない | **時間差あり** |
| 3 | 1段目・候補とも反応していない、注目度が高い | ⚠ 無視されている疑い |
| 4 | 1段目・候補とも反応していない、注目度が低い | 未発見 |
| 5 | 注目度が低いのに候補が反応した | 早耳の先回り(1より優先して表示する) |

1段目に上場企業がない場合は、§6.4 で指定した銘柄群または業種指数を1段目とみなす。

### 6.7 レポート(Python・自動)

- **O1 連想レポート**:`CONCEPT.md` §9.1 の構成で、Markdown と HTML を出力する。
- **O3 連携ファイル**:`data/export/picks_YYYYMMDD.csv`。列は日付、銘柄コード、社名、シナリオ番号、自信度、崩れる条件。

### 6.8 翌朝と翌日の自動処理(Python)

- **翌朝**:その夜のシナリオで1段目に米国企業がある場合、米国市場の終値の動きを確認する。普段の値動きの2倍を超えていれば、通知を出す。
- **翌日の引け後**:
  - **等級の確定**:1段目の銘柄(または指定した銘柄群・業種指数)の TOPIX 超過が(ザラ場中のニュースは当日、引け後のニュースは翌営業日に反応するので、その2日のうち大きいほうで)、普段の値動き(20日の標準偏差)の何倍かを計算する。§7 の閾値で、暫定の等級を確定・修正する。見落としの可能性があれば、翌日のレポートに出す。1段目を指定できなかった場合は、暫定の等級をそのまま確定とし、「検算なし」の印を付ける。
  - **状態の更新**:織り込み度、鮮度、終了の判定(`CONCEPT.md` §8.3)、起動条件の判定材料の提示。
  - **売りのサイン**:過去に本命とした銘柄について、§8.3 の4つを検出する。
  - **「動いた」の判定**:`pick_tracking` を更新する(§7)。

### 6.9 月次の振り返り(Python の集計 + Claude Code)

1. **Python の集計**:
   - 的中率:銘柄単位(本命のうち「動いた」割合)とシナリオ単位(本命のうち1銘柄でも動いたシナリオの割合)の両方を出す。偶然の場合(同じ期間に全銘柄の中で「動いた」銘柄の割合を、`universe_daily` と株価から計算)と比べる。
   - 先行性:最初に挙げた日と「動き出した日」の差。
   - 1段目だけを挙げた場合の的中率・先行性。
   - **見逃しの候補の抽出**:その月の S・A級のイベントごとに、その後20営業日で「動いた」銘柄を全銘柄から抽出する。
   - 運用の状況(`run_log` から)。
2. **Claude Code(`/association-monthly`)**:
   - 見逃しの候補のうち、イベントと関係があるものを判定する(事後の判定として記録する)。これで捕捉率を出す。
   - 見逃しと外れの原因を分類し、直す先(指示文、前提カード、等級の基準、過去事例)を提案する。
   - 月内の大きなイベントを過去事例ライブラリに追加する。
3. 改善を反映したときは、指示文の版を上げ、日付と理由を `DECISIONS.md` に追記する。

### 6.10 過去事例ライブラリ

- **最初の登録(S2)**:大地震、感染症、戦争、政策の大転換、大型の技術ニュースなど、`THEMES.md` のテーマに関係する過去の大きな出来事から、30件程度を選ぶ。
- **値動きの計算は Python**:イベント日から20営業日の TOPIX 超過の最大値、「動いた」かどうか、動き出すまでの日数。
- **関連銘柄の選定と説明は LLM**:上がった銘柄だけでなく、**関連がありながら動かなかった銘柄**も必ず入れる。
- 運用が始まったあとは、月次の振り返りで追加していく(実運用の事例ほど価値が高い)。

### 6.11 テーマの温度と前提カード

- **温度**:当面は、週1回 LLM が「上昇中/横ばい/冷却中」の3段階で更新する。材料は、その週のテーマ関連のニュースの数と、テーマ関連銘柄の値動き。
- **前提カード**:そのテーマで初めて S・A級の判定が出るときに作る(§6.4)。S・A級のニュースが出るたびに、新しい版を作る。

---

## 7. 閾値の初期値

**運用開始前に `ANALYSIS_PLAN.md` で固定する。** 以下は初期値の案である。変更するときは日付と理由を残し、変更の前後を混ぜて評価しない。

| 項目 | 初期値 |
|---|---|
| **「動いた」** | 本命に挙げた日から想定期間内(最長20営業日)に、起点からの TOPIX 超過が **+10%以上**。強化されても期間は延ばさない |
| 的中率の単位 | 日々は銘柄単位、合否の判定はシナリオ単位(本命のうち1銘柄でも動いたシナリオ) |
| 「動き出した日」 | 起点からの TOPIX 超過が初めて +5% を超えた日 |
| 起点(織り込み度) | そのシナリオの起動日の前日終値 |
| 起点(「動いた」) | 本命に初めて挙げた日の終値(レポートは引け後に出すため、その日の値動きは既に知っている) |
| 同じ銘柄の重複 | (シナリオ, 銘柄) の組で最初に本命になった日だけを1件と数える。翌日以降の再掲は数えない |
| 独立したシナリオ | 同じイベントから派生したシナリオは1件と数える |
| 足切り:注目度 | 過去90日のイベントの上位5% |
| 足切り:LLM に渡す件数 | 1日最大3件(進行中シナリオの関係分は別枠) |
| 1段目の「反応した」 | 当日の TOPIX 超過が、普段の値動き(20日の標準偏差)の2倍以上 |
| 注目度が「高い」 | 媒体数が過去90日の上位20% |
| 候補の数 | 1シナリオ最大5銘柄 |
| 本命の数 | 1日最大3銘柄 |
| 進行中・待機中のシナリオ | 最大15件 |
| 単発型の想定期間 | 10営業日(LLM がシナリオごとに提案し、5〜20の範囲に収める) |
| 織り込み完了 | 織り込み度80%以上 |
| 起動しなかった | 待機のまま60営業日 |
| 流動性の下限 | 20日平均の売買代金 1億円(未決。§12) |
| 極端な低位株 | 株価100円未満 |
| 直近で急騰済み | 20営業日で +50%超 |
| ★ | 自信度の絶対値で4段階(閾値は S2 のサンプル出力を見て決める) |

---

## 8. 本命の除外条件と売買の目安

### 8.1 本命にしない条件(Python が機械的に判定)

- 流動性の下限を満たさない
- 監理・整理ポストの銘柄
- 株価100円未満
- 20営業日で +50%超(すでに急騰している)
- 裏取りの結果、すべての矢印が信頼度3・4の情報源だけに基づいている

該当する銘柄は、ランキングには表示するが、本命にはしない(除外の理由を表示する)。

### 8.2 売買の目安(本命カードに表示)

| 項目 | 内容 |
|---|---|
| 決算発表まで | 営業日数。想定期間内にあれば ⚠ を付け、崩れる条件にも自動で加える |
| 権利落ち日 | 想定期間内にあれば表示する |
| 信用規制 | 日々公表銘柄・増担保規制の有無 |
| 普段の1日の値動き | 20日の日次変動の標準偏差 |
| 値幅制限 | JPX の規則による、その銘柄のストップ高・ストップ安の幅 |

### 8.3 売りのサイン(過去に本命とした銘柄)

- 崩れる条件に当たるニュースが出た
- シナリオが弱体化・消滅した(悪材料)
- 織り込み度が70%以上になった(完了が近い。80%でシナリオは終了)
- 想定期間の終わりが近い(残り2営業日以下)

---

## 9. 運用

### 9.1 毎日の予定

| 時刻 | 内容 | 担当 |
|---|---|---|
| 1時間ごと | ニュース・TDnet の収集 | 自動 |
| 18:00 | 株価の計算、足切り、入力パックの作成 | 自動 |
| 20:00〜 | `/association-daily`(30分以内。時間がなければ最小手順) | 手動 |
| 夜 | 確定・計算・レポート作成 | 自動 |
| 翌 7:30 | 米国の1段目の確認と通知 | 自動 |
| 翌 16:00 | 等級の確定、状態の更新、売りのサイン | 自動 |

### 9.2 実行の記録と偏りの点検

- 実行したか、手順の種類(通常・最小)、所要時間、実行しなかった理由を毎日記録する。
- 月次の振り返りで、実行しなかった日に大きなイベントが偏っていないかを確認する。偏っていると、指標が歪む。

### 9.3 作業時間が30分を超える場合

次の順に対処する。
1. LLM に渡すイベントを1日2件に減らす
2. 裏取りを上位1件に減らす
3. 進行中のシナリオの上限を10件に減らす

---

## 10. ディレクトリ構成(案)

```
association-model/
├── docs/                  CONCEPT / DESIGN / DECISIONS / THEMES / ANALYSIS_PLAN
├── config/                RSS の一覧、パス(既存の株価DBの場所など)、閾値
├── schemas/               JSON スキーマ
├── prompts/               指示文(版管理)
├── .claude/commands/      association-daily / association-monthly
├── knowledge/             前提カード、過去事例ライブラリ、指標ライブラリ(git 管理)
├── src/
│   ├── ingest/            収集
│   ├── events/            イベント化・足切り
│   ├── market/            株価の計算(既存DBの読み取り)、1段目の反応、売買の目安
│   ├── master/            銘柄マスタ、別名辞書、上場銘柄一覧、カレンダー、除外条件
│   ├── pack/              入力パック
│   ├── commit/            検証・照合・確定
│   ├── scoring/           鮮度・織り込み度・診断・自信度・本命
│   ├── tracking/          等級の確定、状態の更新、売りのサイン、「動いた」の判定
│   ├── review/            月次の集計
│   └── report/            O1・O2・O3 の出力
├── tests/
└── data/                  ← .gitignore(DB、生データ、レポート)
    ├── inbox/             LLM の出力
    ├── records/           追記専用の記録(別途バックアップ)
    └── export/            O3 連携ファイル
```

---

## 11. 開発フェーズ

各フェーズの終了条件は共通で、次のすべてを満たすこととする。
- 実装
- テスト
- サンプル出力
- 失敗時の動作確認
- 再現手順の記述
- §13 の形式での報告

| # | フェーズ | 主な成果物 | 場所 |
|---|---|---|---|
| **S1** | **既存資産の確認と収集の開始** | 下記の確認結果。ニュース・TDnet・上場銘柄一覧・Wikipedia・GDELT の収集スクリプトと自動実行 | ローカル |
| **S2-a** | データ層 | スキーマ、DuckDB、銘柄マスタと別名辞書、カレンダー、除外条件、株価の計算、イベント化と足切り、入力パック | クラウドで作り、ローカルで実行 |
| **S2-b** | 夜の手順 | `/association-daily`(通常・最小)、指示文 v1、確定処理、計算、O1・O3 レポート、実行の記録 | 同上 |
| **S2-c** | 翌日の処理と過去事例 | 等級の確定、状態の更新、売りのサイン、「動いた」の判定、過去事例ライブラリ30件 | 同上 |
| **S2-d** | 運用前の固定 | `ANALYSIS_PLAN.md`(閾値と「動いた」の定義)、★の閾値、バックアップ | 同上 |
| **S3** | 運用と振り返り | 毎日の運用、月次の振り返り(`/association-monthly`)、3か月で L1 を判定 | ローカル |
| **S4** | 判定と連携 | L2 の判定、既存システムとの連携の検討 | ローカル |

### S1 の確認項目(ローカルの Claude Code で実施)

1. yfinance の株価DB:場所、形式、対象銘柄、更新時刻、欠損、株式分割の調整、上場廃止銘柄が残るか
2. 既存システムの銘柄マスタ(業種、時価総額)、営業日カレンダー
3. 既存の需給システムの信用残・出来高のデータ
4. 既存システムに、ニュース・TDnet・EDINET 関係のコードやデータがあるか
5. 再利用できる共通処理(銘柄コードの正規化など)
6. 既存の大化け株研究のデータ(過去事例ライブラリの素材として)
7. 触ってはいけない箇所の一覧
8. 株探・kabukarin.net の利用規約と robots.txt(自動取得してよいか)
9. 内閣官房「日本成長戦略」の17分野の正式表記(`THEMES.md` の要確認事項)

**確認が終わるまで、収集以外のコードは書かない。** 収集だけは、データが消えていくので先に始める。

---

## 12. 未決事項

| # | 論点 | 選択肢 | 決める時期 |
|---|---|---|---|
| 1 | リポジトリ | このリポジトリで開発するか、既存システムのリポジトリに同居させるか | S1 の確認後 |
| 2 | 流動性の下限 | 20日平均の売買代金で1億円/3億円 など | S2-d |
| 3 | ★の閾値 | サンプル出力を見て決める | S2-d |
| 4 | バックアップ先 | クラウドストレージの種類 | S2-b |
| 5 | テーマの参考サイトの自動取得 | 利用規約しだい | S1 |
| 6 | 決算発表の予定の取得元 | 無料で安定して取れる情報源 | S2-a |

---

## 13. 報告の形式(各フェーズの終了時)

1. 実装したもの
2. 作成・変更したファイル
3. データ件数(機械的な集計による)
4. テスト結果
5. 発見した問題
6. 未解決事項
7. 次のフェーズへ進めるか
8. 現時点で確認できたこと
9. 現時点では確認できないこと

---

## 14. 禁止事項

- 判断の時点で知り得なかった情報を使うこと
- 記録の書き換え(版を追記せずに変えること)
- LLM の推論を証拠として扱うこと
- 過去事例を LLM に解かせた結果を、探す能力の証明に使うこと
- 上がった事例だけで過去事例ライブラリを作ること
- 結果を見てから閾値や「動いた」の定義を変え、変更の前後を混ぜて評価すること
- 銘柄コードと社名の照合をせずに候補を確定すること
- 既存システムのコード・データの変更や上書き
- 件数を推測で書くこと

---

## 付録A. 旧版(v0.3)からの主な変更

| 旧版 | v1.0 | 理由 |
|---|---|---|
| 売買成績の評価(手数料、ストップ高の約定、出口の比較) | 削除 | ゴールを「探す能力」に再定義した |
| 対照群4種類、単純な方法5種類との比較 | 「偶然の的中率」と「1段目だけの場合」の2つに絞った | どちらも記録から後で計算でき、毎日の記録が不要 |
| 検証の問い20個 | 3つの指標(的中率・捕捉率・先行性)と見逃しの分析 | 改善に直結するものに絞った |
| 事例集は後回し・発見用に限定 | 過去事例ライブラリを中核に格上げ | 過去の反応は投資家の連想の元ネタそのもの |
| 毎日の裏取り | 新しい情報が出たときだけ | 作業時間の削減。何も起きていなければ調べる必要はない |
| テーマの5段階(誕生〜冷却) | 新テーマ候補の目印だけ | 新しいテーマの誕生はまれ。大半は既存テーマ × サプライズ |
| 保有銘柄の入力 | 不要。過去に本命とした銘柄に売りのサインを出す | 入力の手間なく、売るべきニュースに対応できる |
| 有効期限を日数で一律に決める | 時間の型と起動・終了の条件 | 賞味期限は事例によって違う |
| 格付け A/B/C | 本命・監視と除外条件 | 自信度ランキングに一本化した |
| 週1回の簡易な答え合わせ | 月1回の振り返りのみ | 件数が少ない時期の早まった判断を避ける |
